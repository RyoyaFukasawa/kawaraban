# 中間生成物（コミット不要）
digest-input.json

# raw-items.json は GitHub Actions(fetch-feeds) が生成し、
# Claude routine がそれを読んで要約するため、あえて追跡する。

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store

# 注意: digest.db はあえて追跡する（検索の正本のため）
